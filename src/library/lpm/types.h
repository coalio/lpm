#pragma once
#include <map>
#include <vector>
#include <string>

// A map of strings to strings.
typedef std::map<std::string, std::string> args_t;
typedef std::map<std::string, std::vector<std::string>> args_map_t;